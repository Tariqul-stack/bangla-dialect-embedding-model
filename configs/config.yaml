model:
  hidden_dim: 256
  num_layers: 4
  dropout: 0.1

training:
  batch_size: 32
  learning_rate: 1e-4
  epochs: 10
  seed: 42

data:
  train_path: data/processed/train.txt
  val_path: data/processed/val.txt
  max_seq_len: 128

tokenizer:
  name: "sagorsarker/bangla-bert-base"